/**
 * Revive Hogar — configuración (copia como config.js)
 * La IA usa GEMINI_API_KEY solo en Vercel (Settings → Environment Variables), no aquí.
 */
window.RH_CONFIG = {
    SUPABASE_URL: '',
    SUPABASE_ANON_KEY: '',
    LOGIN_REQUIERE_PASSWORD: true,
    PASSWORD_INICIAL: '1234'
};
